components:
  schemas:
    Expansions:
      type: object
      properties:
        id:
          type: number
          example: 1
        name:
          type: string
        armies:
          type: array
          items: 
            $ref: '#/components/schemas/Armies'
        active:
          type: boolean