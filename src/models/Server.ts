export type ServerUrl = {
  host?: string;
  port?: string | number;
}

export type ApiPaths = {
  docs: string;
  expansions: string;
  armies: string;
  unitTypes: string;
  users: string;
  roles: string;
  authentication: string;
}