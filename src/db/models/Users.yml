components:
  schemas:
    Users:
      type: object
      properties:
        id:
          type: number
          example: 1
        username:
          type: string
        email:
          type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time